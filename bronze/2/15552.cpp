#include <stdio.h>

int main()
{

	auto T = 0;
	scanf("%d", &T);

	for(int i = 0; i< T;++i){
		if (T >= 1 && 1 <= 1000000){
			int a, b;
			scanf("%d %d", &a, &b);
			printf("%d\n", a+b);
		}
	}
	return 0;
}
